# Railway runtime requirements for Synapse
# Uses external embedding APIs instead of heavy local models

# Core FastAPI dependencies
fastapi>=0.110.0
uvicorn>=0.29.0
python-multipart>=0.0.9
python-dotenv>=1.1.0

# Essential utilities
requests>=2.32.0
pydantic>=2.0.0
numpy>=1.24.0

# External API clients (lightweight)
openai>=1.0.0
cohere>=4.0.0

# Document processing
pypdf>=4.0.0
python-docx>=1.1.0
beautifulsoup4>=4.12.2
markdown>=3.6

# Vector database (lightweight)
faiss-cpu>=1.7.4

# File handling
aiofiles>=23.2.0

# Note: No heavy ML dependencies (torch, transformers, sentence-transformers)
# These are replaced with external embedding APIs to stay under 4GB
